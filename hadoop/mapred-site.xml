<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<!-- http://hadoop.apache.org/docs/current/hadoop-project-dist/hadoop-common/ClusterSetup.html -->
<configuration>

    <property>
        <name>mapreduce.framework.name</name>
        <value>yarn</value>
    </property>

    <property>
        <name>yarn.app.mapreduce.am.env</name>
        <value>HADOOP_MAPRED_HOME=$HADOOP_HOME</value>
    </property>

    <property>
        <name>mapreduce.map.env</name>
        <value>HADOOP_MAPRED_HOME=$HADOOP_HOME</value>
    </property>

    <property>
        <name>mapreduce.reduce.env</name>
        <value>HADOOP_MAPRED_HOME=$HADOOP_HOME</value>
    </property>

    <!-- https://hadoop.apache.org/docs/r3.1.2/hadoop-yarn/hadoop-yarn-site/ResourceModel.html -->
    <!-- The amount of memory a container needs to run MR Application Master -->
    <property>
        <name>yarn.app.mapreduce.am.resource.mb</name>
        <value>1024</value>
    </property>

    <!-- Upper memory limit that Hadoop allows to be allocated to a mapper container, in megabytes. The default is 1024 -->
    <property>
        <name>mapreduce.map.memory.mb</name>
        <value>512</value>
    </property>

    <!-- Upper memory limit that Hadoop allows to be allocated to a reducer container, in megabytes. The default is 1024 -->

    <property>
        <name>mapreduce.reduce.memory.mb</name>
        <value>512</value>
    </property>

</configuration>
